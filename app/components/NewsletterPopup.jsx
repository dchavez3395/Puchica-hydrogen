/**
 * Newsletter collection is intentionally inactive until the consent-first
 * Klaviyo flow is approved. Retain the export so an accidental legacy import
 * cannot surface a marketing-consent form.
 */
export function NewsletterPopup() {
  return null;
}
