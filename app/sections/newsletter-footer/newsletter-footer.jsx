/**
 * Newsletter collection is intentionally inactive until the consent-first
 * Klaviyo flow is approved. Retain the export so older composition code fails
 * closed if this section is imported accidentally.
 */
export function NewsletterFooter() {
  return null;
}
